# Frontend CORS
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000
# Keep false for normal CPU inference. Set true on CUDA machine if torch/onnxruntime-gpu are installed.
PREFER_GPU=false
MAX_UPLOAD_MB=15
RISK_COUNTERFEIT_THRESHOLD=0.68
RISK_SUSPICIOUS_THRESHOLD=0.40
